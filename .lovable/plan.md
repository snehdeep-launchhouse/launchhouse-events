

## Update Favicon and Browser Tab Title

Two small changes to `index.html`:

### 1. Tab Title
Update all title/meta references from "Lovable App" to "LaunchHouse Events" (or similar).

### 2. Favicon
Since you haven't provided a custom favicon image, I'll use the existing Logo component's "LH" monogram design to create an SVG favicon. This keeps branding consistent with your site.

### Technical Details

**File: `index.html`**
- Change `<title>` from "Lovable App" to "LaunchHouse Events"
- Update `og:title` meta tag to "LaunchHouse Events"
- Update `meta description` to match your business
- Replace the favicon reference with an SVG favicon featuring the LH monogram in your brand blue (#006AE1)

**New file: `public/favicon.svg`**
- SVG favicon with the "LH" monogram on a #006AE1 blue background, matching your site logo

No other files will be touched.

